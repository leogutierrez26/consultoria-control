{
  "name": "consultoria-control",
  "version": "1.5.0",
  "private": true,
  "description": "Consultoría Control - Plataforma web de gestión de consultoría (clientes, proyectos, actividades, agenda, reservas, horas, comunicaciones, reportes y auditoría).",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace server",
    "db:migrate": "npm run migrate --workspace server",
    "up": "docker compose up --build -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "dev": "npm run dev --workspace server & npm run dev --workspace web"
  },
  "license": "UNLICENSED"
}
